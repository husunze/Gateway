CREATE DATABASE localdb;

CREATE TABLE trust(IP VARCHAR(20) NOT NULL, key VARCHAR(20) NOT NULL, PRIMARY KEY (IP));
CREATE TABLE TrustKey(IP VARCHAR(20) NOT NULL, key VARCHAR(20) NOT NULL, PRIMARY KEY (IP));